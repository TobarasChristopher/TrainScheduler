<!DOCTYPE html>
<html>
  <head>
    <style>
        body {
          font-family: "Courier New", Courier, monospace;
          color: #ffc107;
          text-align:center;
        }
      </style>
  </head>
  <body class="bg-black">


    <div class="p-3 m-2 bg-black text-white border-bottom border-top border-secondary">
      <div class="container-fluid p-3 bg-dark text-warning text-center ">
        <p style="color: green"><%= notice %></p>

        <%= render @schedule %>

        <div>
          <%= link_to "Edit this schedule", edit_schedule_path(@schedule) %> |
          <%= link_to "Back to schedules", schedules_path %>

          <%= button_to "Destroy this schedule", @schedule, method: :delete %>
        </div>
      </div>
    </div>


  </body>
<html>