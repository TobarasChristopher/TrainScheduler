<!DOCTYPE html>
<html>
  <head>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    
    <style>
      body {
        font-family: "Courier New", Courier, monospace;
        color: #ffc107;
      }
    #clock {
      position: absolute;
      top: 205px;
      right: 50px;
      font-size: 32px;
      color: #ffc107;
      
    }
    </style>
  </head>
    <body class="bg-black">
    
      <div class="p-3 mb-2 bg-black">
        <div id="clock"></div>
        <div class="p-3 mb-2 bg-black text-white border-bottom border-secondary">
          <div class="container-fluid p-3 bg-dark text-warning text-center ">
            <h1>Train Scheduler</h1>
            <p>A helpful reminder for making it on time!</p> 
          </div>
        
          <div class="container-fluid p-2 bg-black text-warning text-center">
          </div>

          <p style="color: green"><%= notice %></p>
          <div class="container-fluid p-3 bg-dark text-warning text-center">
            <h1 class="border-bottom border-black pb-2">Timetable</h1>
            

            <div id="schedules" >
              <% @schedules.each do |schedule| %>
                <%= render schedule %>
                <p class="border-bottom border-black pb-2">
                  <%= link_to "Show this schedule", schedule %>
                </p>
              <% end %>
            </div>



            <%= link_to "New schedule", new_schedule_path %>
          </div>

        </div>
      </div>
      <script>
        function updateClock() {
          var now = new Date();
          var hours = now.getHours();
          var minutes = now.getMinutes();
          var seconds = now.getSeconds();

          // Add leading zero if needed
          hours = (hours < 10) ? "0" + hours : hours;
          minutes = (minutes < 10) ? "0" + minutes : minutes;
          seconds = (seconds < 10) ? "0" + seconds : seconds;

          var timeString = hours + ":" + minutes + ":" + seconds;

          document.getElementById("clock").innerHTML = timeString;
        }

        // Update the clock every second
        setInterval(updateClock, 1000);

        // Initial update
        updateClock();
      </script>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.j" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    </body>
  </html>